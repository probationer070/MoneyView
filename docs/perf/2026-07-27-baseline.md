# Performance Baseline — 2026-07-27

## Environment
watchlist: 139 tickers · stocks: 67459 rows · db: 11.0 MB
event limit: 20000 · compute mode: in_process
git: f4753b3

## Measurement conditions
Three in-process conditions, disclosed here because they change what is being
measured, and two reports are only comparable if these match (spec 08.2):

- **OHLCV freshness frozen** (`_rows_are_fresh` and `_rows_cover_period` forced true):
  cached price rows are served without a live yfinance refetch. Without this, a stale
  local dataset makes every request refetch the whole universe, measuring network
  latency and delisted-ticker 404s instead of the DB+compute fan-out. Production, on
  current daily data, takes the frozen path.
- **Global rate limiter neutralised**: the runner fires bursts no real user would, and
  429s would otherwise truncate a scenario partway through.
- **A discarded priming pass runs before pass A.** Process-level state (the statement
  cache, SQLite page cache, lazily imported modules) outlives a single pass, so without
  priming whichever pass ran first paid first-touch costs the later ones inherited warm.
  That biased the overhead comparison enough to produce negative percentages. A residual
  ordering effect may remain, which is why a negative overhead is now stamped INVALID
  rather than PASS.
- **Statement cache TTL raised to 86400s** (default 300s) **and maxsize to
  4096** (default 48), so the untimed warm-up's 138 live Yahoo fetches survive into
  the timed iterations. At the defaults the cache scores a measured **0% hit rate** on this
  fan-out, for two independent reasons: one 138-ticker sweep takes ~357s so ticker #1
  expires before #138 is fetched, and maxsize 48 < 139 tickers so the sweep evicts its own
  first 90 entries anyway. Without both raised the timed samples measure Yahoo network
  latency, not the DB+compute fan-out. **Production still runs the defaults**, so the p50
  below is the warm-cache cost and is NOT what a user experiences today -- see ERROR-LOG.md.

## Overhead (criterion 1: <= 3%)
| scenario | p50 off | p50 on | overhead | |
| --- | --- | --- | --- | --- |
| portfolio_page_load | 530.5ms | 643.4ms | 21.3% | FAIL |
| comparison_138 | 53899.4ms | 53953.7ms | 0.1% | PASS |
| attribution_138 | 511.7ms | 631.7ms | 23.5% | FAIL |
| single_stock_detail | 52.8ms | 54.5ms | 3.2% | FAIL |
| tab_switch | 741.5ms | 827.6ms | 11.6% | FAIL |

**Why overhead varies so widely between scenarios:** instrumentation cost scales
with the number of spans emitted, not with request duration. A short request that
emits thousands of spans pays a far larger percentage than a long one that emits few.
Read each row against its span count below before concluding anything about the code.

## Scenario: portfolio_page_load
p50 643.4 ms · p95 700.5 ms · N=20
mean 650.7 ms · stdev 35.4 ms · MAD 23.0 ms · 95% CI [635.2, 666.2] ms
emitted 8640 events / 8560 spans (428 spans per iteration)

### Scope breakdown (self time) — criterion 2: unattributed <= 15%
| scope | self_ms | pct |
| --- | --- | --- |
| api | 12191.2 | 95.1% |
| db | 634.8 | 4.9% |
| cache | 0.0 | 0.0% |
| unattributed | 0.0 | 0.0% | PASS

### Top spans by self time
_`parent` rows are for tracing, not optimisation: a parent's self time is only what its children did not account for. See the ranked bottlenecks below, which use leaves._
| operation | kind | self_ms | count | per-call | pct of request |
| --- | --- | --- | --- | --- | --- |
| api.request_start | parent | 12191.2 | 80 | 152.4 ms | 95.1% |
| db.select_stocks | leaf | 610.4 | 2780 | 0.2 ms | 4.8% |
| db.select_watchlist | leaf | 17.5 | 80 | 0.2 ms | 0.1% |
| db.select_corporate_companies | leaf | 2.4 | 20 | 0.1 ms | 0.0% |
| db.select_portfolio_preferences | leaf | 2.3 | 20 | 0.1 ms | 0.0% |
| db.select_dataset_metadata | leaf | 2.2 | 20 | 0.1 ms | 0.0% |
| cache.lookup | leaf | 0.0 | 2780 | 0.0 ms | 0.0% |
| cache.hit | leaf | 0.0 | 2780 | 0.0 ms | 0.0% |

### Critical path (slowest request)
_Self time says where CPU goes; the critical path says what determines latency. They differ wherever work overlaps — concurrent fetches can hold little CPU yet dominate elapsed time. This is the chain that has to get shorter for the request to get faster._
slowest request: 704.8 ms

api.request_start — 704.8 ms · 100% of request
  └─ db.select_stocks — 1.5 ms · 0% of request

### Attributed self-time per ticker
_Self time attributed to spans carrying a ticker — not end-to-end latency for that stock. A ticker whose work happens inside spans that carry no ticker will read as 0.0 ms here._
139 tickers · distribution: uniform (cv 0.0)
p50 0.0 ms · p95 0.0 ms · max 0.0 ms
outliers (>p95): 0
**0 of 139 tickers carry measured self time**; the other 139 appear only in zero-duration events, which is what pulls the median toward 0.0 and inflates cv. Not an instrumentation failure, but the percentiles above are not a per-stock cost distribution.

### Cache effectiveness
| component | hits | misses | hit_rate | avg_miss_ms | est. saved_ms |
| --- | --- | --- | --- | --- | --- |
| market_data.ohlcv | 2780 | 0 | 1.0 | 0.0 | 0.0 |

_Every `avg_miss_cost_ms` is 0.0 because cache miss events carry no `duration_ms`: the miss cost and the time saved are **unmeasured, not zero**. The hit and miss counts above are real. Timing the miss path is the next span._

### Diagnostics
orphans: 0 · partial: False · truncated: False · overlap_detected: False — criterion 3: PASS
reproducibility delta 0.6% — criterion 4: PASS

## Scenario: comparison_138
p50 53953.7 ms · p95 54275.9 ms · N=10
mean 53953.3 ms · stdev 251.8 ms · MAD 212.0 ms · 95% CI [53797.3, 54109.4] ms
emitted 12600 events / 12580 spans (1258 spans per iteration)

### Scope breakdown (self time) — criterion 2: unattributed <= 15%
| scope | self_ms | pct |
| --- | --- | --- |
| external | 516300.6 | 95.7% |
| calculation | 22785.8 | 4.2% |
| db | 281.9 | 0.1% |
| api | 124.0 | 0.0% |
| metric | 0.3 | 0.0% |
| cache | 0.0 | 0.0% |
| unattributed | 0.0 | 0.0% | PASS

### Top spans by self time
_`parent` rows are for tracing, not optimisation: a parent's self time is only what its children did not account for. See the ranked bottlenecks below, which use leaves._
| operation | kind | self_ms | count | per-call | pct of request |
| --- | --- | --- | --- | --- | --- |
| external.fetch_quote | leaf | 516300.6 | 1400 | 368.8 ms | 95.7% |
| ticker.metrics | parent | 14670.7 | 1400 | 10.5 ms | 2.7% |
| ticker.price | parent | 7167.0 | 1400 | 5.1 ms | 1.3% |
| fanout.comparison | parent | 715.2 | 10 | 71.5 ms | 0.1% |
| calculation.dcf_upside | parent | 232.9 | 1400 | 0.2 ms | 0.0% |
| db.select_corporate_metrics | leaf | 141.4 | 1400 | 0.1 ms | 0.0% |
| db.select_watchlist | leaf | 135.0 | 1300 | 0.1 ms | 0.0% |
| api.request_start | parent | 124.0 | 10 | 12.4 ms | 0.0% |
| db.select_corporate_comparison_snapshots_v3 | leaf | 2.5 | 20 | 0.1 ms | 0.0% |
| db.select_stocks | leaf | 2.0 | 10 | 0.2 ms | 0.0% |

### Critical path (slowest request)
_Self time says where CPU goes; the critical path says what determines latency. They differ wherever work overlaps — concurrent fetches can hold little CPU yet dominate elapsed time. This is the chain that has to get shorter for the request to get faster._
slowest request: 54320.2 ms

api.request_start — 54320.2 ms · 100% of request
  └─ fanout.comparison — 54304.3 ms · 100% of request
    └─ calculation.dcf_upside — 2021.9 ms · 4% of request
      └─ ticker.price — 2021.7 ms · 4% of request
        └─ external.fetch_quote — 1277.1 ms · 2% of request

### Attributed self-time per ticker
_Self time attributed to spans carrying a ticker — not end-to-end latency for that stock. A ticker whose work happens inside spans that carry no ticker will read as 0.0 ms here._
140 tickers · distribution: mixed (cv 0.3658)
p50 3630.0 ms · p95 4303.8 ms · max 20134.5 ms
outliers (>p95): 7

### Cache effectiveness
| component | hits | misses | hit_rate | avg_miss_ms | est. saved_ms |
| --- | --- | --- | --- | --- | --- |
| corporate_statement_bundle | 1400 | 0 | 1.0 | 0.0 | 0.0 |
| market_data.ohlcv | 10 | 0 | 1.0 | 0.0 | 0.0 |

_Every `avg_miss_cost_ms` is 0.0 because cache miss events carry no `duration_ms`: the miss cost and the time saved are **unmeasured, not zero**. The hit and miss counts above are real. Timing the miss path is the next span._

### Diagnostics
orphans: 0 · partial: False · truncated: False · overlap_detected: False — criterion 3: PASS
reproducibility delta 0.5% — criterion 4: PASS

## Scenario: attribution_138
p50 631.7 ms · p95 702.3 ms · N=10
mean 650.6 ms · stdev 38.6 ms · MAD 24.8 ms · 95% CI [626.7, 674.6] ms
emitted 4250 events / 4230 spans (423 spans per iteration)

### Scope breakdown (self time) — criterion 2: unattributed <= 15%
| scope | self_ms | pct |
| --- | --- | --- |
| api | 6107.1 | 94.8% |
| db | 322.9 | 5.0% |
| calculation | 9.0 | 0.1% |
| cache | 0.0 | 0.0% |
| unattributed | 0.0 | 0.0% | PASS

### Top spans by self time
_`parent` rows are for tracing, not optimisation: a parent's self time is only what its children did not account for. See the ranked bottlenecks below, which use leaves._
| operation | kind | self_ms | count | per-call | pct of request |
| --- | --- | --- | --- | --- | --- |
| api.request_start | parent | 6107.1 | 20 | 305.4 ms | 94.8% |
| db.select_stocks | leaf | 317.6 | 1390 | 0.2 ms | 4.9% |
| calculation.portfolio_attribution | parent | 9.0 | 10 | 0.9 ms | 0.1% |
| db.select_watchlist | leaf | 5.3 | 20 | 0.3 ms | 0.1% |
| cache.lookup | leaf | 0.0 | 1390 | 0.0 ms | 0.0% |
| cache.hit | leaf | 0.0 | 1400 | 0.0 ms | 0.0% |

### Critical path (slowest request)
_Self time says where CPU goes; the critical path says what determines latency. They differ wherever work overlaps — concurrent fetches can hold little CPU yet dominate elapsed time. This is the chain that has to get shorter for the request to get faster._
slowest request: 710.8 ms

api.request_start — 710.8 ms · 100% of request
  └─ db.select_stocks — 0.9 ms · 0% of request

### Attributed self-time per ticker
_Self time attributed to spans carrying a ticker — not end-to-end latency for that stock. A ticker whose work happens inside spans that carry no ticker will read as 0.0 ms here._
139 tickers · distribution: uniform (cv 0.0)
p50 0.0 ms · p95 0.0 ms · max 0.0 ms
outliers (>p95): 0
**0 of 139 tickers carry measured self time**; the other 139 appear only in zero-duration events, which is what pulls the median toward 0.0 and inflates cv. Not an instrumentation failure, but the percentiles above are not a per-stock cost distribution.

### Cache effectiveness
| component | hits | misses | hit_rate | avg_miss_ms | est. saved_ms |
| --- | --- | --- | --- | --- | --- |
| market_data.ohlcv | 1390 | 0 | 1.0 | 0.0 | 0.0 |
| portfolio.attribution_cache | 10 | 0 | 1.0 | 0.0 | 0.0 |

_Every `avg_miss_cost_ms` is 0.0 because cache miss events carry no `duration_ms`: the miss cost and the time saved are **unmeasured, not zero**. The hit and miss counts above are real. Timing the miss path is the next span._

### Diagnostics
orphans: 0 · partial: False · truncated: False · overlap_detected: False — criterion 3: PASS
reproducibility delta 2.7% — criterion 4: PASS

## Scenario: single_stock_detail
p50 54.5 ms · p95 56.6 ms · N=20
mean 54.8 ms · stdev 1.3 ms · MAD 0.8 ms · 95% CI [54.3, 55.4] ms
emitted 520 events / 440 spans (22 spans per iteration)

### Scope breakdown (self time) — criterion 2: unattributed <= 15%
| scope | self_ms | pct |
| --- | --- | --- |
| api | 661.9 | 73.6% |
| calculation | 151.0 | 16.8% |
| metric | 85.6 | 9.5% |
| db | 4.0 | 0.4% |
| cache | 0.0 | 0.0% |
| data_quality | 0.0 | 0.0% |
| unattributed | 0.0 | 0.0% | PASS

### Top spans by self time
_`parent` rows are for tracing, not optimisation: a parent's self time is only what its children did not account for. See the ranked bottlenecks below, which use leaves._
| operation | kind | self_ms | count | per-call | pct of request |
| --- | --- | --- | --- | --- | --- |
| api.request_start | parent | 661.9 | 80 | 8.3 ms | 73.6% |
| ticker.metrics | parent | 147.0 | 20 | 7.3 ms | 16.4% |
| metric.roic | leaf | 42.5 | 20 | 2.1 ms | 4.7% |
| metric.metric_audit | parent | 29.9 | 20 | 1.5 ms | 3.3% |
| metric.growth | leaf | 13.2 | 20 | 0.7 ms | 1.5% |
| db.select_corporate_metrics | leaf | 4.0 | 40 | 0.1 ms | 0.4% |
| calculation.roic_minus_wacc | leaf | 4.0 | 20 | 0.2 ms | 0.4% |
| cache.lookup | leaf | 0.0 | 80 | 0.0 ms | 0.0% |
| cache.hit | leaf | 0.0 | 80 | 0.0 ms | 0.0% |
| metric.wacc | leaf | 0.0 | 20 | 0.0 ms | 0.0% |

### Critical path (slowest request)
_Self time says where CPU goes; the critical path says what determines latency. They differ wherever work overlaps — concurrent fetches can hold little CPU yet dominate elapsed time. This is the chain that has to get shorter for the request to get faster._
slowest request: 29.6 ms

api.request_start — 29.6 ms · 100% of request
  └─ cache.lookup — 0.0 ms · 0% of request

### Attributed self-time per ticker
_Self time attributed to spans carrying a ticker — not end-to-end latency for that stock. A ticker whose work happens inside spans that carry no ticker will read as 0.0 ms here._
1 tickers · distribution: uniform (cv 0.0)
p50 236.6 ms · p95 236.6 ms · max 236.6 ms
outliers (>p95): 0

### Cache effectiveness
| component | hits | misses | hit_rate | avg_miss_ms | est. saved_ms |
| --- | --- | --- | --- | --- | --- |
| corporate_statement_bundle | 80 | 0 | 1.0 | 0.0 | 0.0 |

_Every `avg_miss_cost_ms` is 0.0 because cache miss events carry no `duration_ms`: the miss cost and the time saved are **unmeasured, not zero**. The hit and miss counts above are real. Timing the miss path is the next span._

### Diagnostics
orphans: 0 · partial: False · truncated: False · overlap_detected: True — criterion 3: PASS
reproducibility delta 1.7% — criterion 4: PASS
**`overlap_detected: True` is bad here, and it invalidates criterion 2.** It means children's self time sums past their parent's duration, so the scope percentages above exceed 100%. The cause is that `page_load.*` spans measure the *same interval* as `api.request_*` while being nested under them, double-counting the whole request. Because spec 04.7 forces `unattributed_ms = 0` whenever overlap is detected, criterion 2 prints PASS while the true figure is not computable. Recorded in ERROR-LOG.md; not yet fixed.

## Scenario: tab_switch
p50 827.6 ms · p95 1018.5 ms · N=20
mean 857.2 ms · stdev 88.7 ms · MAD 54.3 ms · 95% CI [818.3, 896.0] ms
emitted 9100 events / 9040 spans (452 spans per iteration)

### Scope breakdown (self time) — criterion 2: unattributed <= 15%
| scope | self_ms | pct |
| --- | --- | --- |
| api | 15254.8 | 89.8% |
| db | 1741.6 | 10.2% |
| cache | 0.0 | 0.0% |
| unattributed | 0.0 | 0.0% | PASS

### Top spans by self time
_`parent` rows are for tracing, not optimisation: a parent's self time is only what its children did not account for. See the ranked bottlenecks below, which use leaves._
| operation | kind | self_ms | count | per-call | pct of request |
| --- | --- | --- | --- | --- | --- |
| api.request_start | parent | 15254.8 | 60 | 254.2 ms | 89.8% |
| db.select_indices | leaf | 1098.6 | 180 | 6.1 ms | 6.5% |
| db.select_stocks | leaf | 623.5 | 2780 | 0.2 ms | 3.7% |
| db.select_watchlist | leaf | 17.3 | 80 | 0.2 ms | 0.1% |
| db.select_corporate_companies | leaf | 2.2 | 20 | 0.1 ms | 0.0% |
| cache.lookup | leaf | 0.0 | 2960 | 0.0 ms | 0.0% |
| cache.hit | leaf | 0.0 | 2960 | 0.0 ms | 0.0% |

### Critical path (slowest request)
_Self time says where CPU goes; the critical path says what determines latency. They differ wherever work overlaps — concurrent fetches can hold little CPU yet dominate elapsed time. This is the chain that has to get shorter for the request to get faster._
slowest request: 827.4 ms

api.request_start — 827.4 ms · 100% of request
  └─ db.select_stocks — 0.6 ms · 0% of request

### Attributed self-time per ticker
_Self time attributed to spans carrying a ticker — not end-to-end latency for that stock. A ticker whose work happens inside spans that carry no ticker will read as 0.0 ms here._
148 tickers · distribution: uniform (cv 0.0)
p50 0.0 ms · p95 0.0 ms · max 0.0 ms
outliers (>p95): 0
**0 of 148 tickers carry measured self time**; the other 148 appear only in zero-duration events, which is what pulls the median toward 0.0 and inflates cv. Not an instrumentation failure, but the percentiles above are not a per-stock cost distribution.

### Cache effectiveness
| component | hits | misses | hit_rate | avg_miss_ms | est. saved_ms |
| --- | --- | --- | --- | --- | --- |
| market_data.ohlcv | 2960 | 0 | 1.0 | 0.0 | 0.0 |

_Every `avg_miss_cost_ms` is 0.0 because cache miss events carry no `duration_ms`: the miss cost and the time saved are **unmeasured, not zero**. The hit and miss counts above are real. Timing the miss path is the next span._

### Diagnostics
orphans: 0 · partial: False · truncated: False · overlap_detected: False — criterion 3: PASS
reproducibility delta 2.4% — criterion 4: PASS

## Ranked bottlenecks (criterion 5)
_Leaf spans only. A parent's self time is whatever its children did not account for, so ranking parents names a call tree rather than code to change._

### portfolio_page_load — fan-out distribution: uniform
1. db.select_stocks — 610.4 ms self across 2780 calls (0.2 ms/call, 5% of request)
2. db.select_watchlist — 17.5 ms self across 80 calls (0.2 ms/call, 0% of request)
3. db.select_corporate_companies — 2.4 ms self across 20 calls (0.1 ms/call, 0% of request)
4. db.select_portfolio_preferences — 2.3 ms self across 20 calls (0.1 ms/call, 0% of request)
5. db.select_dataset_metadata — 2.2 ms self across 20 calls (0.1 ms/call, 0% of request)
   -> structural fix indicated (batched queries, per-ticker memoization, or parallelism); the per-stock table is not worth reading row by row

### comparison_138 — fan-out distribution: mixed
1. external.fetch_quote — 516300.6 ms self across 1400 calls (368.8 ms/call, 96% of request)
2. db.select_corporate_metrics — 141.4 ms self across 1400 calls (0.1 ms/call, 0% of request)
3. db.select_watchlist — 135.0 ms self across 1300 calls (0.1 ms/call, 0% of request)
4. db.select_corporate_comparison_snapshots_v3 — 2.5 ms self across 20 calls (0.1 ms/call, 0% of request)
5. db.select_stocks — 2.0 ms self across 10 calls (0.2 ms/call, 0% of request)
   -> both, ranked by the top-spans table: outlier tickers first, then the structural fix

### attribution_138 — fan-out distribution: uniform
1. db.select_stocks — 317.6 ms self across 1390 calls (0.2 ms/call, 5% of request)
2. db.select_watchlist — 5.3 ms self across 20 calls (0.3 ms/call, 0% of request)
3. cache.lookup — 0.0 ms self across 1390 calls (0.0 ms/call, 0% of request)
4. cache.hit — 0.0 ms self across 1400 calls (0.0 ms/call, 0% of request)
   -> structural fix indicated (batched queries, per-ticker memoization, or parallelism); the per-stock table is not worth reading row by row

### single_stock_detail — fan-out distribution: uniform
1. metric.roic — 42.5 ms self across 20 calls (2.1 ms/call, 5% of request)
2. metric.growth — 13.2 ms self across 20 calls (0.7 ms/call, 1% of request)
3. db.select_corporate_metrics — 4.0 ms self across 40 calls (0.1 ms/call, 0% of request)
4. calculation.roic_minus_wacc — 4.0 ms self across 20 calls (0.2 ms/call, 0% of request)
5. cache.lookup — 0.0 ms self across 80 calls (0.0 ms/call, 0% of request)
   -> structural fix indicated (batched queries, per-ticker memoization, or parallelism); the per-stock table is not worth reading row by row

### tab_switch — fan-out distribution: uniform
1. db.select_indices — 1098.6 ms self across 180 calls (6.1 ms/call, 6% of request)
2. db.select_stocks — 623.5 ms self across 2780 calls (0.2 ms/call, 4% of request)
3. db.select_watchlist — 17.3 ms self across 80 calls (0.2 ms/call, 0% of request)
4. db.select_corporate_companies — 2.2 ms self across 20 calls (0.1 ms/call, 0% of request)
5. cache.lookup — 0.0 ms self across 2960 calls (0.0 ms/call, 0% of request)
   -> structural fix indicated (batched queries, per-ticker memoization, or parallelism); the per-stock table is not worth reading row by row
